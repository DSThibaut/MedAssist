<%= simple_form_for @medical_care_medicament do |f| %>
  <%= @medical_care_medicament.medicament.name %>
  <%= f.input :dose %>
  <%= f.select :frequence, MedicalCareMedicament.frequences.keys %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
